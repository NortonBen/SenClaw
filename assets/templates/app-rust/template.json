{
  "name": "app-rust",
  "kind": "app",
  "lang": "rust",
  "description": "Space App viết bằng Rust (axum): REST + MCP + UI tĩnh, chạy theo phiên",
  "variables": [
    {
      "name": "crate_name",
      "description": "Tên crate và tên binary",
      "default": "{{snake_name}}"
    }
  ],
  "postCreate": [
    "cargo run                       # chạy thử: http://127.0.0.1:{{port}}",
    "./scripts/pack.sh               # đóng gói -> {{id}}-app.zip",
    "Cài vào SenClaw: Plugins → Space Apps → Install from zip"
  ]
}
