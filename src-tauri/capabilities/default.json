{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the chat and main windows",
  "windows": ["chat", "main"],
  "permissions": ["core:default", "positioner:default"]
}
