{
  "name": "google-calendar-space-app",
  "private": true,
  "scripts": {
    "build": "next build && node scripts/copy-manifest.mjs",
    "pack:zip": "npm run build && node scripts/remove-zip.mjs && cd out && zip -r ../google-calendar-space-app.zip ."
  },
  "dependencies": {
    "@senclaw/space-sdk": "file:../senclaw-space-sdk",
    "next": "^15.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
