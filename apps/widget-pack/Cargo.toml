[package]
name = "widget-pack"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
axum = "0.7"
serde_json = "1"
tower-http = { version = "0.5", features = ["fs", "cors"] }
